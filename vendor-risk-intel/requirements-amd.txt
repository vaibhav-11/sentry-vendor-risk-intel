# ── Install base requirements first ───────────────────────────────────────────
# pip install -r requirements.txt
# pip install -r requirements-amd.txt

# ── ROCm / AMD MI300X ─────────────────────────────────────────────────────────
# PyTorch with ROCm 6.x — install via the official AMD wheel index:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.1

# ── vLLM with ROCm support ────────────────────────────────────────────────────
# Install from source or use the ROCm-enabled release:
# pip install vllm  (the standard wheel now ships with ROCm support)
vllm>=0.4.2

# ── Transformers / HuggingFace (for embedding on GPU) ─────────────────────────
transformers>=4.40.0
accelerate>=0.29.0
bitsandbytes>=0.43.0    # quantisation helpers

# ── Flash Attention (AMD-compatible via ROCm) ─────────────────────────────────
# pip install flash-attn --no-build-isolation  (only on AMD box, not Mac)

# ── PEFT / LoRA (for optional fine-tuning sprint on Day 3) ────────────────────
peft>=0.10.0
datasets>=2.18.0
trl>=0.8.6
